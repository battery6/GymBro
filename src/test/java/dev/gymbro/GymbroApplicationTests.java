package dev.gymbro;

import org.junit.jupiter.api.Test;

class GymbroApplicationTests extends AbstractIntegrationTest {

    @Test
    void contextLoads() {
        // Verifies the full context (security, JPA, Flyway, Redis autoconfig) starts.
    }
}
